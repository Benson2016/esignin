/** * 显示模式窗口 * @param url 窗口地址 * @param dName 窗口名称 * @param dTitle 窗口标题 * @param dWidth 窗口宽度 * @param dHeight 窗口高度 */function showDialog(url, dName, dTitle, dWidth, dHeight, callback){    hyxt.dialog.show({        src : url,        name : dName,        title : dTitle,        width : dWidth,        height : dHeight,        after_close: callback    });}/** * 显示带Form表单的模式窗口 * @param url 窗口地址 * @param dName 窗口名称 * @param dTitle 窗口标题 */function showFormDialog(url, dName, dTitle, callback){    showDialog(url, dName, dTitle, 610, 710, callback);}